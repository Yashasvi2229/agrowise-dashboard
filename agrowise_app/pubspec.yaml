name: agrowise_app
description: "AgroWise - Agricultural Advisory Mobile App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core dependencies
  cupertino_icons: ^1.0.8
  camera: ^0.10.5+5
  image_picker: ^1.0.4
  http: ^1.1.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  permission_handler: ^11.0.1
  intl: any
  path_provider: ^2.1.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
